import { {{camelCase name}} } from './{{kebabCase name}}';

describe('{{camelCase name}}', () => {
  it('{{camelCase name}} should return correct value', () => {
    const output = {{camelCase name}}({});

    expect(output).toMatchInlineSnapshot();
  });
});
